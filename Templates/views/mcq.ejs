<%- include('header'); -%>
    <!-- Navbar - End -->
    <!-- MCQ - Start -->
    <div class="wrapper mb-4 ">
        <div class="container card-body align-self-center">
            <% for(var j=0;j<use1.length;j++) { %> 
            <h6><%= use1[j].description %></h6>
            <hr>
            <p>Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium doloremque
                laudantium, totam rem aperiam, eaque ipsa quae ab illo inventore veritatis et quasi
                architecto beatae vitae dicta sunt explicabo.</p>
            <form action="/mcq" method="POST">
            <div class="">
                <div class="checkbox form-check my-3">
                 <% for(var i=0; i<= use1[j].alternatives.length; i++) { %>
                    <input class=" checkbox form-check-input" type="checkbox" name="ans"
                        id="flexRadioDefault1" value="<%= use1[j].alternatives[i].text %>">
                <label class="checkbox form-check-label" for="flexRadioDefault1" name="ans" >
                    <%= use1[j].alternatives[i].text %>
                    <% i++ %>
                </label>
                </div>
                <div class="checkbox form-check my-3">
                    <input class=" checkbox form-check-input" type="checkbox" name="ans"
                        id="flexRadioDefault1" value="<%= use1[j].alternatives[i].text %>">
                <label class="checkbox form-check-label" for="flexRadioDefault1" name="ans" >
                    <%= use1[j].alternatives[i].text %>
                    <% i++ %>  
                </label>
                </div>
                <div class="checkbox form-check my-3">
                    <input class=" checkbox form-check-input" type="checkbox" name="ans"
                        id="flexRadioDefault1" value="<%= use1[j].alternatives[i].text %>">
                <label class="checkbox form-check-label" for="flexRadioDefault1" name="ans" >
                    <%= use1[j].alternatives[i].text %>
                    <% i++ %>
                </label>
                </div>
                <div class="checkbox form-check my-3">
                    <input class=" checkbox form-check-input" type="checkbox" name="ans"
                        id="flexRadioDefault1" value="<%=use1[j].alternatives[i].text %>">
                <label class="checkbox form-check-label" for="flexRadioDefault1" name="ans" >
                    <%= use1[j].alternatives[i].text %>
                    <% i++ %> 
                </label>
                </div>
                <% } %>
            </div>
            <% j++ %> 
            <% }%>
            <div class="d-flex justify-content-center">
                <button type="submit" class="btn rounded-pill  btn-dark px-4">Continue</button>
            </div>
            </form>
        </div>
    </div>
    <!-- MCQ - End -->
    <script>
    window.onload = function () {
    var minute = 50;
    var sec = 60;
    setInterval(function () {
        document.getElementById("timer").innerHTML = minute + " : " + sec;
        sec--;
        if (sec == 00) {
            minute--;
            sec = 60;
            if (minute == 0) {
                minute = 5;
            }
        }
    }, 1000);
}
    </script>
</body>
</html>